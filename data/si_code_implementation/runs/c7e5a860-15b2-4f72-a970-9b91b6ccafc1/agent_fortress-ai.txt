Implemented the `duplicate_entry_accumulation` fix in **fortress-ai** (complete) and staged the **trading-bot** Classic exit chunking via a deploy script (cross-repo writes are blocked in this session).

## Summary

The unified agent was stacking entries on symbols already held (e.g. SMH ×3). The fix adds entry deduplication, chunked exits under `FORTRESS_MAX_ORDER_NOTIONAL_USD`, and legacy position flattening.

### fortress-ai (applied)

| File | Change |
|------|--------|
| `unified_ai/position_manager.py` | `enter_position()` blocks duplicates when symbol held; **new** `flatten_oversized_positions()` plans chunked trims |
| `unified_ai/order_executor.py` | Chunks large exits under notional cap |
| `unified_ai/legacy_flattener.py` | `flatten_oversized_positions()` for oversized legacy positions |
| `unified_ai/risk_controller.py` | `flatten_legacy_positions()` on boot + every 5 min |
| `agents/unified_ai_agent.py` | Wires dedup/chunking; random 0.1–0.5s delays between chunked order submits |
| `utils/unified_enter_guard.py` | Cooldown + `already_holding` when observation lags broker |
| `utils/order_chunking.py` | Shared chunk helper; reads `config.defaults` when env unset |
| `config/defaults.py` + `config/default.yaml` | `FORTRESS_MAX_ORDER_NOTIONAL_USD=50000`, `POSITION_DEDUPLICATION_ENABLED=true` |
| `utils/si_fix_deployment.py` | Added `order_chunking.py` + `defaults.py` to marker scan paths |
| `unified_ai/tests/test_*.py` + `tests/unified_ai/test_position_deduplication.py` | Dedup, chunking, flatten, and act() integration tests |

**Pre-trade gate dedup (plan item 4):** `utils/pre_trade_gate.py` is protected and was not edited. Deduplication runs upstream in `PositionManager.enter_position()` and `utils/unified_enter_guard.py` before any broker submit — same effect, no gate weakening.

### trading-bot (staged, not yet applied)

Cross-repo file writes failed in this environment. Deploy script ready:

```bash
python3 /home/ubuntu/fortress-ai/scripts/apply_trading_bot_order_sizer.py
```

This creates:
- `trading-bot/utils/order_sizer.py` — `plan_chunked_exit()` + `submit_chunked_sell_orders()` with random inter-chunk delays
- `trading-bot/tests/test_order_sizer.py` — 6 unit tests
- Patches `orchestrator.py` `execute_sell_order()` to use chunked exits

### Detectable markers

| Marker | Where |
|--------|-------|
| `already_holding` | `position_manager.py` logs + `block_reason`; `unified_enter_guard.py` |
| `chunked_exit` | `order_executor.py`, `legacy_flattener.py`, `risk_controller.py`, `act()` result |
| `enter_cooldown` | `position_manager.py` logs + `block_reason` |
| `entry_blocked_by_cooldown` | Log text in `position_manager.py`; `entry_blocked_by_cooldown()` in `unified_enter_guard.py` |

### Behavior

1. **Entry dedup** — When `POSITION_DEDUPLICATION_ENABLED=true` (default), `enter_position()` returns blocked if symbol is already held.
2. **Chunked exits** — Exits exceeding `FORTRESS_MAX_ORDER_NOTIONAL_USD` split into child orders (e.g. 447 shares @ $200 with $3k cap → multiple 15-share orders).
3. **Legacy flatten** — On agent boot and every 5 minutes, oversized positions are trimmed via chunked sells.

`config/si_fix_registry.json` already lists all four markers for `duplicate_entry_accumulation` — no registry change needed.

### Next step for SI runner

1. Run `./scripts/e2e_verify.sh --no-ingest` in fortress-ai
2. Run `python3 scripts/apply_trading_bot_order_sizer.py` for Classic exit chunking
3. Commit after e2e passes (per your instruction, no commit was made here)
