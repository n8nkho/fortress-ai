{
  "version": 1,
  "description": "Bounded meta-knobs the capability reviewer may auto-tune (never weakens pre-trade gate or immutable caps).",
  "capabilities": {
    "winning_pattern_share_target": {
      "kind": "tunable",
      "bounds": { "min": 0.4, "max": 0.85 },
      "default": 0.75,
      "apply_via": "data/si_capability/overrides.json",
      "reader": "utils.skim_swarm_config.target_winning_pattern_share",
      "notes": "Phase-1 realistic target when portfolio patterns are net negative."
    },
    "edge_autofix_rr_boost_cap": {
      "kind": "tunable",
      "bounds": { "min": 0.08, "max": 0.25 },
      "default": 0.2,
      "apply_via": "data/si_capability/overrides.json",
      "notes": "Caps session RR margin boost from edge autofix."
    },
    "edge_autofix_min_exits": {
      "kind": "tunable",
      "bounds": { "min": 3, "max": 12 },
      "default": 4,
      "apply_via": "data/si_capability/overrides.json",
      "notes": "Minimum exits before edge autofix fires (reduce noise on quiet days)."
    },
    "rth_review_cadence_mult": {
      "kind": "tunable",
      "bounds": { "min": 0.5, "max": 1.0 },
      "default": 1.0,
      "apply_via": "data/si_capability/overrides.json",
      "notes": "Multiplier on RTH SI interval when objectives off-track (<1 = more frequent)."
    },
    "session_si_critical_expectancy_usd": {
      "kind": "tunable",
      "bounds": { "min": -0.75, "max": -0.05 },
      "default": -0.15,
      "apply_via": "data/si_capability/overrides.json",
      "notes": "Session expectancy threshold for critical mode (more negative = less aggressive)."
    },
    "rolling_edge_autofix_strength": {
      "kind": "tunable",
      "bounds": { "min": 0.15, "max": 1.0 },
      "default": 0.55,
      "apply_via": "data/si_capability/overrides.json",
      "notes": "Scales rolling-aware edge autofix when session scorecard is green but rolling metrics lag."
    },
    "symbol_session_loss_brake_mult": {
      "kind": "tunable",
      "bounds": { "min": 0.5, "max": 2.0 },
      "default": 1.0,
      "apply_via": "data/si_capability/overrides.json",
      "notes": "Multiplier on per-symbol session loss brake thresholds."
    },
    "unified_loser_trim_pct_equity": {
      "kind": "tunable",
      "bounds": { "min": 0.02, "max": 0.12 },
      "default": 0.05,
      "apply_via": "data/si_capability/overrides.json",
      "notes": "Unrealized loss as fraction of equity before adaptive partial trim."
    },
    "unified_loser_trim_fraction": {
      "kind": "tunable",
      "bounds": { "min": 0.1, "max": 0.5 },
      "default": 0.25,
      "apply_via": "data/si_capability/overrides.json",
      "notes": "Fraction of position to trim when loser threshold exceeded."
    },
    "classic_fill_recency_days_max": {
      "kind": "tunable",
      "bounds": { "min": 3.0, "max": 14.0 },
      "default": 7.0,
      "apply_via": "data/si_capability/overrides.json",
      "notes": "Adaptive max days since last Classic fill before recency gap fires."
    },
    "singularity_aggression_mult": {
      "kind": "tunable",
      "bounds": { "min": 0.5, "max": 1.5 },
      "default": 1.0,
      "apply_via": "data/si_capability/overrides.json",
      "notes": "Scales surpass-mode SI escalation when floor objectives are met."
    },
    "singularity_aspire_lift_pct": {
      "kind": "tunable",
      "bounds": { "min": 0.02, "max": 0.15 },
      "default": 0.05,
      "apply_via": "data/si_capability/overrides.json",
      "notes": "Fraction to lift dynamic aspire targets each singularity compound cycle."
    }
  }
}
