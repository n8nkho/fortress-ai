# Example: TLS + reverse proxy to Flask (Fortress dashboard).
# Copy ideas into /etc/nginx/sites-available/, obtain certs (certbot), then:
#   FORTRESS_AI_DASHBOARD_HOST=127.0.0.1
#   FORTRESS_AI_DASHBOARD_PORT=8050
# so Flask only listens on localhost; nginx listens on 443.
#
# Replace YOUR_DOMAIN with a real hostname. Test with: sudo nginx -t

# server {
#     listen 443 ssl http2;
#     server_name YOUR_DOMAIN;
#
#     ssl_certificate     /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8050;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_buffering off;
#         proxy_read_timeout 86400;
#     }
# }
